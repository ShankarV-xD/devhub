import React from "react";
// Functionality moved to ColorRenderer.tsx

interface ColorToolsProps {
  content: string;
  searchTerm?: string;
  setContent?: (val: string) => void;
}

export const ColorTools: React.FC<ColorToolsProps> = () => {
  return null;
};
